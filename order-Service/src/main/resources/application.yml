spring:
  application:
    name: order-Service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///iddmohammad
    username: root
    password: Idd@709395

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  open-in-view: false
  properties:
    hibernate:
      format_sql: true

server:
  port: 8083

product:
  service:
    url: http://localhost:8082
user:
  service:
    url: http://localhost:8081